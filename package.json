{
  "name": "test-arv-security",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Extension MV3 para auditoria defensiva de seguridad web.",
  "scripts": {
    "dev": "vite build --watch",
    "build": "vite build && node scripts/copy-manifest.mjs",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.292",
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
